# План миграции RiotGalaxy с CocosSharp на MonoGame

## Общий подход

Миграция будет выполняться поэтапно, с сохранением работоспособности проекта на каждом шаге. После завершения каждого этапа мы должны иметь рабочую версию игры с минимальной функциональностью.

## Этап 1: Базовый игровой цикл и главный экран

### 1.1. Каркас главного экрана игры - Создать базовый класс GameManager ✅
- Реализовать простой игровой цикл Update/Draw ✅
- Создать базовый экран игры ✅
- Создать заголовок и базовый UI (заглушки) ✅
- **Результат**: Рабочее окно игры с управлением состояниями

### 1.2. Отображение корабля игрока ✅
- Реализовать класс PlayerShip с базовой отрисовкой ✅
- Загрузить и отобразить спрайт корабля игрока ✅
- Разместить корабль в нижней части экрана ✅
- **Результат**: Корабль игрока отображается на экране ✅

### 1.3 ✅ Базовый главный цикл игры ЗАВЕРШЕНО
- Адаптировать основной цикл игры из GamePlay.cs
- Создать список игровых объектов
- Реализовать базовый Update/Draw для всех объектов
- **Результат**: Каркас игрового цикла с возможностью добавлять объекты
- Улучшен алгоритм обработки объектов
- Добавлена базовая система игровых событий
- **7 улучшений реализовано:**
  - Система проверки завершения игры (победа/поражение)
  - Оптимизированный алгоритм обработки объектов
  - Система отложенного удаления объектов
  - Расширенная инициализация игрового процесса
  - Диагностика и исправление ошибок
- **Дата завершения**: 2026-05-22 12:35:42

## Этап 2: Загрузка ассетов (шрифты, звуки, спрайты)

Перенос контента из старого проекта в MonoGame Content Pipeline и подготовка ассетов к загрузке в коде.

### 2.1. Content Pipeline и спрайты ✅ (2026-06-04)
- Включён MonoGame Content Pipeline: установлен локальный инструмент `dotnet-mgcb` 3.8.1.303 (`.config/dotnet-tools.json`), в `RiotGalaxy.DesktopGL.csproj` добавлена `MonoGameContentReference` с `ContentFolder=Content` ✅
- Перенесены спрайты из старого атласа `CocoSharp/.../Images/images.png` + `images.plist` (способ C — нарезка на отдельные PNG скриптом `MonoGame/tools/split_atlas.py`): 26 спрайтов → `RiotGalaxy.Content/Images/*.png`, собираются в `.xnb` ✅
- **Загрузка:** `Content.Load<Texture2D>("Images/<имя>")`, например `"Images/ship"`
- **Результат**: спрайты собираются в `.xnb` и доступны для загрузки ✅

### 2.2. Шрифты ✅ (2026-06-04)
- `TestFont.spritefont` собирается через Content Pipeline; шрифт `Segoe UI Mono` (нет на Linux) заменён на `DejaVu Sans Mono` ✅
- **Загрузка:** `Content.Load<SpriteFont>("TestFont")`
- **Результат**: шрифт собирается в `.xnb` и доступен для загрузки ✅

### 2.3. Звуки ✅ (2026-06-04)
- Перенесены SFX `explode1.wav`, `fire1.wav` → `RiotGalaxy.Content/Sounds/`, добавлены в `.mgcb` (WavImporter / SoundEffectProcessor), собираются в `.xnb` ✅
- BGM в старом проекте отсутствует (`FruityFalls` упоминается, но файла нет) — переносить нечего
- Добавлен `AudioManager` (`Managers/AudioManager.cs`): загрузка `Sounds/fire1`, `Sounds/explode1`, `PlayEffect(key)`, громкость `0.1` (как в оригинале). Загружается в `GameManager.LoadContent`
- Фактические вызовы `PlayEffect` (на выстрел/взрыв) — в этапах 4 (оружие) и 7 (бои), как в оригинале
- **Результат**: звуки собираются и доступны для загрузки ✅

### 2.4. Фоновые изображения ✅ (2026-06-04)
- Перенесены `background.jpg` (1280×720), `background_blue.jpg` (1280×768), `SCConvoy_0.png` (512×512) → `RiotGalaxy.Content/Backgrounds/`, добавлены в `.mgcb`, собираются в `.xnb` ✅
- `background_blue` (точно под разрешение 1280×768) грузится в `GameManager` и рисуется задником во всех состояниях
- **Результат**: фоны доступны для загрузки ✅

### 2.5. Использование ассетов в игровых объектах 🔶 (корабль игрока ✅, 2026-06-04)
- Заменить временные заглушки (`CreateSimpleTexture` — цветные квадраты) реальными спрайтами
- **Корабль игрока** ✅: добавлен `PlayerShip.LoadContent(ContentManager)` — грузит `Images/ship` (61×61) в `Texture`; вызывается из `GameManager.InitializeGameplay`. Заглушка `CreateSimpleTexture` оставлена как фолбэк. `SetGraphicsDevice` больше не создаёт зелёный квадрат
  - Проверено: сборка зелёная, `ship.xnb` валиден (Texture2DReader, 61×61). Визуально в окне не проверено — в среде нет дисплея
  - Примечание: задача 1.2 была помечена выполненной, но рисовалась заглушка — теперь подключён настоящий спрайт
- **Осталось** ⬜: остальные объекты (враги, снаряды, бонусы, щит) получат спрайты по мере реализации их классов в этапах 4–8
- **Результат**: корабль игрока отображается реальным спрайтом ✅

## Этап 3: Управление игроком

### 3.1 ✅ Система ввода
- Реализовать InputManager для обработки клавиатуры и мыши ✅
- Добавить базовую поддержку touch (заглушка для мобильных) ✅
- Интегрировать систему ввода в игровой цикл ✅
- **Результат**: Система ввода реагирует на нажатия клавиш и мыши ✅
- **Комментарии**: Создана точная адаптация системы InputHandler из CocosSharp, включая поддержку команд и GUI кнопок

### 3.2 ✅ Движение корабля игрока
- Реализовать компонент движения PlayerMovementComponent ✅
- Добавить ограничение движения в пределах экрана ✅
- Реализовать плавное движение корабля ✅
- **Результат**: Корабль игрока может двигаться по экрану ✅
- **Комментарии**: Создана полная адаптация ObjBehPlayerMove из CocosSharp с системой ускорения/торможения и физикой движения

### 3.3 ✅ Состояния игрока
- Добавить здоровье игрока ✅
- Реализовать базовые состояния (жив/мертв) ✅
- Добавить визуальную обратную связь (временная неуязвимость) ✅
- **Результат**: Игрок имеет здоровье и состояние ✅
- **Комментарии**: Создана полная система здоровья с временным щитом (неуязвимостью) и событиями

## Этап 4: Система оружия и снаряды

### 4.1. Базовая система оружия ✅ (2026-06-04)
- Иерархия `Weapon` (`Weapons/Weapon.cs`): база + `WeaponCannon`, `WeaponMinigun`, `WeaponLaser`, `NoWeapon` ✅
- `WeaponOptions` (`Weapons/WeaponOptions.cs`): burst, burstInterval, reloadSpeed, damage, shellSpeed — копия из CocosSharp ✅
- `WeaponConfig` (`Weapons/WeaponConfig.cs`): параметры 3 уровней, перенесены 1:1 из `CocoSharp/.../weapon.ini` (TODO: вернуть загрузку из файла на этапе конфигов) ✅
- Логика очередей/перезарядки (burst + burstInterval + reloadSpeed) реализована в `Weapon.Update/Fire` (вместо CocosSharp Schedule), прицеливание `Aim` с учётом инверсии оси Y ✅
- **Результат**: система оружия готова к использованию ✅

### 4.2. Снаряды ✅ (2026-06-04)
- Базовый `Shell` (`GameObjects/Shell.cs`) + `Bullet` (cannon), `Slug` (minigun), `Laser` (пробивающий) ✅
- Движение прямолинейное в `Shell.Update` (Direction*Speed*dt) + самоуничтожение за экраном (аналог Shell.Activity) ✅
- Визуализация: спрайты `Images/bullet`, `Images/slug`, `Images/laser` через базовый `GameObject.Draw` (поворот по направлению) ✅
- **Результат**: снаряды отображаются и движутся по экрану ✅

### 4.3. Стрельба игрока ✅ (2026-06-04)
- `PlayerShip.Gun` (текущее оружие) + `PlayerShip.Fire()`; `Gun.Update` в цикле игрока ✅
- Ввод: стрельба по удержанию Space (темп держит оружие), смена оружия клавишами 1/2/3 — в `InputManager` ✅
- Темп стрельбы и очереди — через burst/reloadSpeed оружия; звук `fire1` на старте очереди ✅
- Снаряды добавляются в `GameManager.GameObjects` и обрабатываются общим циклом Update/Draw ✅
- **Результат**: игрок может стрелять снарядами ✅
- **Проверка**: сборка зелёная (0 ошибок); визуально в окне ещё не проверено (нужен запуск через run_game.sh)
- **Примечание**: урон/попадания по врагам — на этапе 7 (столкновения); враги ещё не реализованы

## Этап 5: Враги

### 5.1. Базовый класс врага ✅ (2026-06-04)
- `Enemy : GameObject` (`GameObjects/Enemy.cs`): тип, Hp/MaxHp, Damage, MaxSpeed/CurrentSpeed, `TakeDamage`/`Die` (звук explode1), загрузка спрайта ✅
- `EnemyType { RND, SM_SCOUT, BLUE, GREEN, RED }` как в оригинале ✅
- Базовый AI: пока движение по траектории (полноценный ИИ/стрельба — этап 7) ✅
- **Результат**: базовый класс врагов с простой логикой ✅

### 5.2. Типы врагов ✅ (2026-06-04)
- `EnemySmallBlue` (Hp10, вниз, speed130), `EnemySmallGreen` (Hp10, смена курса каждые 2с), `EnemySmallRed` (Hp20, медленный), `EnemySmallScout` (Hp10, урон5, случайный уклон) ✅
- Спрайты `Images/enemyBlue|enemyGreen|enemyRed|enemySmallScout` ✅
- **Результат**: различные типы врагов с уникальными характеристиками ✅

### 5.3. Движение врагов ✅ (2026-06-04)
- `EnemyBounceMovement` (`Components/EnemyBounceMovement.cs`): отскок от боковых границ (поле −10%) + телепорт снизу-вверх (аналог BehEnMoveStandartBounce/SlideUp) ✅
- `SetDirection(угол)` — вектор скорости от направления (с учётом инверсии Y) ✅
- Стартовая волна из 7 врагов спавнится в `GameManager.SpawnInitialObjects` ✅
- **Результат**: враги двигаются по заданным траекториям ✅
- **Проверка**: сборка зелёная; визуально не проверено (нужен запуск)
- **Примечание**: урон по врагам/стрельба врагов — этап 7 (пока снаряды пролетают сквозь)

## Этап 6: Столкновения и бои

### 6.1. Система столкновений ✅ (2026-06-04)
- `GameManager.ProcessCollision` реализован: диспетчер пар объектов (аналог GameObject.Collision из CocosSharp), вызывается из двойного цикла `ProcessGameObjects` по пересечениям (AABB через `GetBounds`/`Intersects`) ✅
- Защита от двойной обработки и от ударов «мёртвыми» объектами ✅
- **Результат**: корректная обработка столкновений ✅

### 6.2. Повреждения и уничтожение ✅ (2026-06-04)
- Снаряд игрока → урон врагу (`enemy.TakeDamage`), обычный снаряд исчезает, лазер летит насквозь (IsPiercing) ✅
- Враг-таран → урон игроку (`player.TakeDamage`) + враг уничтожается ✅
- Смерть врага: `Die` → удаление + звук `explode1` + декремент `EnemiesRemaining` (→ Victory при 0) ✅
- Смерть игрока → событие → GameOver ✅
- **Результат**: объекты получают урон и уничтожаются ✅
- **Проверка**: сборка зелёная; визуально не проверено
- **TODO**: визуальный эффект взрыва (в оригинале CCParticleExplosion; частиц в MonoGame нет из коробки — позже)

### 6.3. Враги могут стрелять ✅ (2026-06-04)
- У врага есть оружие `Gun` (WeaponCannon); стреляет по таймеру `ShootInterval` (3с, со случайным стартовым сдвигом) — базовый AI решения о стрельбе ✅
- Паттерны: синий/зелёный — строго вниз (`ShootDown`), красный — прицельно в игрока (`ShootAimAtPlayer`), скаут — не стреляет ✅
- Снаряд получает сторону стрелка (`shell.PlayerSide = owner is PlayerShip`); вражеские снаряды бьют игрока (через `ProcessCollision`), не задевают врагов; звук выстрела только у игрока ✅
- **Результат**: враги атакуют игрока ✅
- **Проверка**: сборка зелёная; визуально не проверено
- **TODO**: визуальный эффект взрыва (частицы) — позже

## Этап 7: Бонусы и награды

### 7.1. Базовая система бонусов ✅ (2026-06-04)
- `Bonus : GameObject` + `BonusHpUp` (хил), `BonusBulletUp` (апгрейд оружия), `BonusNukeBomb` (убить всех), `BonusStar` (очки) ✅
- `BonusType { BULLET_UP, HP_UP, NUKE_BOMB, STAR }` как в оригинале, спрайты `Images/bonus*` ✅
- Выпадение при смерти врага (`GameManager.SpawnBonusOnEnemyDeath`): всегда звезда + 30% шанс усиления (HpUp 45% / BulletUp 45% / Nuke 10%) ✅
- **Результат**: бонусы появляются и могут быть подобраны ✅

### 7.2. Магнит и подбор бонусов ✅ (2026-06-04, доработка 2026-06-09)
- Звезда (`BonusStar`) притягивается к игроку (плавный доворот), вращается; вне радиуса возвращается к падению вниз ✅
- Подбор — в `GameManager.ProcessCollision` (Bonus × PlayerShip → `Apply`), бонус исчезает ✅
- 2026-06-09: магнит вынесен в `weapons.yaml` (`magnet`: radius/pullSpeed/turnSpeed → `WeaponConfig.Magnet`) — оборудование корабля; исправлен баг доворота (нормализация угла через ±180°, иначе на границе звезда разворачивалась «не туда») ✅
- **Результат**: бонусы подбираются игроком; магнит настраивается из файла ✅
- **Примечание**: отдельного звука подбора нет (нет ассета); звук — TODO

### 7.3. Улучшения оружия ✅ (2026-06-04)
- Уровни оружия в `Weapon` (`Level`/`InitLevel`/`Upgrade`), параметры по уровням из `WeaponConfig` ✅
- Сохранение уровней между сменами оружия: `PlayerShip._weaponLevels[тип]`, `UpgradeWeapon()` ✅
- Уровень оружия показан в HUD («Оружие: … (ур. N)») ✅
- **Результат**: игрок может улучшать оружие ✅
- **Примечание**: визуальное различие снаряда по уровням пока не делал (отличаются темп/очередь/перезарядка); индикация уровня — в HUD

## Этап 8: Экран заставки и меню

### 8.1. Экран заставки ✅ (2026-06-04)
- `ScreenSystem` + базовый `Screen` (свой ввод клавиатуры/мыши) в `Screens/` ✅
- `SplashScreen`: логотип `SCConvoy_0` + таймер 2.5с → меню (пропуск любой клавишей/кликом) ✅
- Интеграция через состояния `GameState.Splash/Settings` + маршрутизация Update/Draw в `GameManager`; старт игры теперь со Splash (авто-переход в Playing убран) ✅
- **Результат**: заставка появляется при запуске и переходит в меню ✅

### 8.2. Главное меню ✅ (2026-06-04)
- `MainMenuScreen`: заголовок + кнопки «Начать игру / Настройки / Выход» ✅
- Навигация мышью (наведение+клик) и клавиатурой (стрелки+Enter, Space — старт, Esc — выход) ✅
- **Результат**: главное меню с функциональными кнопками ✅

### 8.3. Экран настроек ✅ (2026-06-04)
- `SettingsScreen`: громкость эффектов (←/→ или кнопки [-]/[+]), «Назад» ✅
- Сохранение в файл `settings.ini` (рядом с приложением) через `Utils/GameSettings`; загрузка при старте ✅
- **Результат**: экран настроек с сохранением параметров ✅

## Этап 9: Уровни и прогрессия

### 9.1. Система уровней ✅ (2026-06-04)
- `Utils/Level.cs`: загрузка `Content/Levels/level{N}.yaml` (YAML-формат: description + events: enemy/count, interval, wait), таймлайн спавна `Tick(dt)`, подсчёт уровней `CountLevels()` ✅
- 5 уровней (level1–5.yaml, перенос из оригинальных .txt), прогрессия сложности 2→6→13→25→42 врага ✅
- GameManager спавнит врагов по таймлайну в `UpdateGameplay`; `EnemiesRemaining` = врагов уровня ✅
- **Результат**: система уровней с прогрессирующей сложностью ✅
- 2026-06-09: level1–5 переработаны в кампанию с нарастанием, задействующую все механики
  (формации+улей с `sortie`, маршруты `parallel`, тактики из enemies.yaml, свободный ИИ, босс):
  L1 First Contact → L2 Recon Snakes → L3 The Swarm → L4 Crossfire → L5 Red Fear (boss)

### 9.2. Экраны между уровнями ✅ (2026-06-04)
- `Screens/NextLevelScreen`: номер уровня, описание, очки; «Пробел/Enter — продолжить» ✅
- Между уровнями игрок и счёт сохраняются (состояние `NextLevel`, `ClearNonPlayerObjects`) ✅
- **Результат**: плавные переходы между уровнями ✅
- **TODO**: сохранение прогресса на диск (возобновление с уровня) — не делал

### 9.3. Экраны победы и поражения ✅ (2026-06-04)
- Victory (все уровни пройдены) / GameOver (смерть): показывают итоговые очки + подсказки ✅
- Перезапуск: Пробел — заново (новая игра с 1 уровня), Esc — в меню ✅
- **Результат**: полный цикл игры (меню → заставка → уровни → победа/поражение → рестарт) ✅

## Этап 10: Мир и формации

### 10.1. Система мира ✅ (2026-06-04)
- `GameObjects/World.cs` (+`Cell`): сетка 16×10 по 90px, отцентрирована на экране, `GetCellPosition(x,y)` ✅
- Создаётся в `GameManager.LoadLevel`; используется для расстановки формаций ✅
- **Результат**: мир с координатной сеткой ✅

### 10.2. Система формаций ✅ (2026-06-04)
- `GameObjects/Hive.cs`: подсетка-улей (8×2) поверх ячеек World; `TryTakeCell`, `CellWorldPos`; синхронное барражирование (дрейф `Offset` по синусу) ✅
- `Components/FormationMovement.cs`: враг летит к своей ячейке и «прилипает», двигаясь вместе с ульем; `Enemy.JoinFormation(hive,cx,cy)` ✅
- Спавн формации из YAML-уровня: `{ enemy: blue, count: 8, formation: true }` (демо — level4 «Hive», 16 врагов) ✅
- **Результат**: враги могут двигаться группами ✅

### 10.3. Миксование паттернов ✅ (2026-06-04)
- Сосуществуют паттерны: отскок (`EnemyBounceMovement`), случайный уклон, формация (`FormationMovement`) — задаются на уровне ✅
- Босс `GameObjects/EnemyBoss.cs`: Hp200, крупный (Scale 2.5), прицельная стрельба, медленное барражирование; в YAML `{ enemy: boss }` (финал level5) ✅
- **Результат**: разнообразное поведение врагов ✅
- **Проверка**: сборка зелёная; визуально не проверено
- **Примечание**: отдельного спрайта босса в атласе нет — используется увеличенный `enemyRed`

## Этап 11: Окончание и полировка

### 11.1. Звуки и музыка
- Адаптировать звуковую систему
- Добавить фоновую музыку
- Реализовать звуковые эффекты
- **Результат**: Полное звуковое сопровождение

### 11.2. Оптимизация
- Оптимизировать производительность
- Добавить профилирование
- Устранить проблемы с производительностью
- **Результат**: Оптимизированная производительность

### 11.3. Финальное тестирование
- Провести полное тестирование
- Исправить ошибки и недочеты
- Подготовить документацию
- **Результат**: Готовая к использованию игра

## Этап 12: Конфиги игровых объектов (YAML)

Вынести параметры игровых объектов из кода в YAML-конфиги (как weapons/options),
с поддержкой рандомизации (диапазоны min/max).

### 12.1. Конфиг врагов ✅ (2026-06-05)
- `Content/Config/enemies.yaml`: на тип (blue/green/red/scout/boss) — hp, damage, speed, shootInterval ✅
- Рандомизация: `speedMin/speedMax`, `shootIntervalMin/shootIntervalMax` (или фиксированные `speed`/`shootInterval`); `shootInterval: 0` — не стреляет ✅
- `Utils/EnemyConfig.cs` (Load + дефолты-фолбэк); `Enemy.ApplyStats(type)` применяет параметры в конструкторах типов ✅
- **Результат**: характеристики врагов настраиваются из файла без перекомпиляции ✅
- **Проверка**: сборка зелёная, enemies.yaml валиден; визуально не проверено

### 12.2. Конфиги прочих объектов 🔶 (в работе)
- Игрок: время неуязвимости после попадания → `options.yaml` `player.invulnTime` (исправлен баг: было «бесконечно» из-за мс/сек) ✅
- Бонусы: `Content/Config/bonuses.yaml` (`hpUpAmount`, `starScore`) + `Utils/BonusConfig` ✅
- Снаряды и прочие параметры — по мере надобности ⬜
- **Результат**: ключевые эффекты/сущности конфигурируются из файлов

## Этап 13: Продвинутые уровни (DSL из оригинала)

Доперенос системы уровней `LvlEventDirector` — то, что в оригинале было, но в текущий
упрощённый `Level` не вошло.

### 13.1. Маршруты врагов (Route) ✅ (2026-06-05)
- `GameObjects/Route.cs`: точки маршрута (коорд. ячеек World), загрузка из `Content/Routes/<name>.yaml` (кэш) ✅
- `Components/RouteMovement.cs`: враг летит от точки к точке; после последней — обычный отскок (остаётся на экране) ✅
- `Enemy.SetRoute(route)`; в YAML-уровне `{ enemy: blue, route: zmeyka1-left }` ✅
- Маршруты `zmeyka1-left/right` перенесены из оригинала (с поправкой на инверсию Y); демо — level2 ✅
- Поведение после маршрута настраивается (`after`): `bounce`/`scatter`/`formation` — чтобы не летели одной колонной ✅
- **Результат**: разнообразные траектории входа врагов ✅
- **Проверка**: сборка зелёная, маршруты валидны; визуально не проверено

### 13.2. Триггеры и синхронные волны ✅ (2026-06-05)
- YAML-эквивалент `trigger:`/`<sync_cmd>` — событие `parallel`: несколько групп-подтаймлайнов спавнятся одновременно, основной таймлайн ждёт их завершения ✅
- `Level` переписан на под-таймлайны (`Timeline`), рекурсивный разбор групп ✅
- Демо — level2 (две змейки слева/справа одновременно, затем красные) ✅
- **Результат**: уровни с синхронными параллельными волнами ✅
- **Проверка**: сборка зелёная, level2 валиден; визуально не проверено

### 13.3. Кат-сцены / радиопереговоры ⬜
- Перенести `CutScene` (`cut_sc:`): сообщения + аватары персонажей (фича из PRD)
- **Результат**: диалоги/кат-сцены между событиями уровня

## Этап 14: ИИ врагов (машина состояний) ✅ (2026-06-08)

### 14.1. Каркас AI (аналог BehAI/AIState) ✅

- `AI/AIState.cs` — состояния TakeOff/Swarming/Attack (Enter/Exit/NewIdea/Update, ReadyToChange)
- `AI/EnemyAI.cs` — контроллер (ChangeState + AI(time): NewIdea раз в ~3с + условия переходов)
- Враг получил опциональный `Ai`, флаг `ShootSafe`, API для состояний (`UseBounceMovement`/`SetMoveDirection`/`SetShootInterval`). Формация/маршрут (YAML) отключают `Ai`
- **Результат**: враги принимают решения по состояниям ✅

### 14.2. Поведение по типам ✅

- `EnemyAIRed` (TakeOff→Swarming), `EnemyAIBlue` (TakeOff→Swarming↔Attack), `EnemyAIDumb`
- **Результат**: разнообразное поведение врагов ✅

### 14.3. Улей: вылеты (Galaga) + тактики ✅ (доп., по просьбе)

- Совмещены формация (улей) и атака: `Hive` координирует «вылеты» — раз в `sortieInterval`
  отправляет `sortieCount` осевших юнитов в пике (`SortieMovement`), затем юнит уходит за низ,
  появляется сверху и возвращается в свою ячейку. Параметры — в описании уровня (`sortie`/`sortieInterval`/`sortieCount`)
- Тактики пике — список `tactics` у типа врага (`enemies.yaml`): `random`/`snake`/`ram`/`ellipse`;
  при каждом вылете выбирается случайно. Скорость вылета — `attackSpeed` (траектории привязаны к ней)
- В строю улья враги **не стреляют** (как в Galaga) — огонь только в вылете (`ShootSafe`)
- **Результат**: рой сидит вверху, по очереди пикирует разными траекториями; проверено на A56

## Этап 15: HUD, сообщения и эффекты

### 15.1. Полноценный HUD ⬜
- Выделить HUD из `GameManager` (отдельный класс), события обновления
- **Результат**: аккуратный отдельный HUD

### 15.2. Лог сообщений / консоль ✅ (2026-06-05)
- `Managers/MessageLog.cs` — всплывающие сообщения над панелью кнопок (затухают), аналог `GUI.Message` ✅
- Сообщения при: смене/апгрейде оружия, бонусах (+HP, +очки, нюк), кнопках (kill all, лечение, след. уровень) ✅
- **Результат**: всплывающие игровые сообщения ✅

### 15.3. Визуальные эффекты (взрывы) ⬜
- Эффект взрыва при гибели (в оригинале `SfxBlast`/частицы — фактически не использовались)
- Реализовать в MonoGame (своя простая система частиц или анимация)
- **Результат**: визуальная обратная связь при уничтожении

## Этап 16: Сборка под Android + отладка

**Окружение ✅ (2026-06-05, собрано в Docker-образе по PRD — хост не засорён):**

- Образ `riotgalaxy-android-build` (5.08 ГБ): база `mcr.microsoft.com/dotnet/sdk:9.0`, JDK 17, Android SDK (cmdline-tools, platform-tools, `platforms;android-35`, `build-tools;35.0.0`), workload `android` 35.0.105/9.0.100
- Файлы: `docker/Dockerfile.android`, `docker/build-image.sh` (сборка образа), `docker/shell.sh` (вход с примонтированными исходниками в `/src`), `.dockerignore`
- Внутри образа: dotnet 9.0.314, java 17.0.19, adb 1.0.41 — проверено
- ⚠️ Workload — для .NET 9 → Android-проект (16.2) таргетить **net9.0-android** (не net6.0-android). MonoGame.Framework.Android тянется как NuGet при первой сборке внутри контейнера
- Пересобрать образ: `cd MonoGame && ./docker/build-image.sh`; войти: `./docker/shell.sh`

### 16.1. Кросс-платформенная загрузка ассетов (ПРЕДУСЛОВИЕ) ✅

- Чтение упакованного контента переведено на `TitleContainer.OpenStream("Content/...")` (работает и на Desktop, и на Android): `Yaml.LoadAsset<T>` + `Yaml.AssetExists` ✅
- Переведены загрузчики: `WeaponConfig`, `EnemyConfig`, `BonusConfig`, `GameOptions` (Config/*.yaml), `Level` (Levels/*.yaml + `CountLevels`), `Route` (Routes/*.yaml) ✅
- `Yaml.ConfigPath`(абс.) → `Yaml.ConfigAsset`(отн.); `Level.LevelPath` → `Level.LevelAsset` ✅
- `settings.yaml` (запись) — пока остаётся `File.WriteAllText(AppContext.BaseDirectory)` (writable-хранилище под Android настроим в 16.2/16.3) 🔶
- Проверено на DesktopGL: `[DBG] Configs loaded. Levels found: 5, player HP: 50` — путь через TitleContainer считывается, сборка зелёная ✅
- **Результат**: чтение read-only ассетов кросс-платформенно; осталась только запись настроек

### 16.2. Проект RiotGalaxy.Android ✅ (2026-06-05)

- Проект `RiotGalaxy.Android` (`net9.0-android`, вне RiotGalaxy.sln) + `MonoGame.Framework.Android` 3.8.1.303 + YamlDotNet ✅
- `MainActivity : AndroidGameActivity` (namespace RiotGalaxy) — landscape, создаёт `Game1.Instance`, `SetContentView(view)`, `Run()` ✅
- Общий код Core НЕ через ProjectReference, а **линковкой исходников** (`<Compile Include="../RiotGalaxy.Core/**/*.cs">`) — Core завязан на DesktopGL-пакет, линковка компилирует тот же код против Android-фреймворка и не ломает desktop/основное решение ✅
- Контент: `MonoGameContentReference` собрал `.xnb` под `/platform:Android` (32 файла); YAML — как `AndroidAsset` под `assets/Content/{Config,Levels,Routes}` (11 файлов) — пути совпали с `TitleContainer.OpenStream` из 16.1 ✅
- В Docker-образ добавлены шрифты (`fontconfig`, `fonts-dejavu-core`, `libfreetype6`) — без них MGCB `FontDescriptionProcessor` падал на `TestFont.spritefont` («DejaVu Sans Mono») ✅
- **Результат**: собирается подписанный (debug) APK `com.riotgalaxy.game-Signed.apk`. Сборка: `./docker/build-apk.sh [Debug|Release]`. Проверена регрессия — desktop-сборка зелёная ✅
- ✅ **Запущено на устройстве Samsung A56 (SM_A566E)**: главное меню с фоном отображается, контент и YAML-конфиги грузятся, `[DBG]`-логи видны в logcat (тег DOTNET). Установка/запуск: `./docker/run-on-device.sh`
- Исправлены 3 бага при первом запуске:
  - `EmbedAssembliesIntoApk=true` для Debug — иначе Fast Deployment кладёт .dll вне APK → `adb install` даёт SIGABRT «No assemblies found»
  - `MonoGameContentReference` → `<ContentFolder>Content</ContentFolder>` — иначе .xnb кладутся в `assets/RiotGalaxy.Content/`, а `ContentManager` ищет в `assets/Content/`
  - `SpriteBatch` создаётся теперь в `GameManager.LoadContent`, а не в `Initialize`/конструкторе — на Android `GraphicsDevice` в конструкторе ещё null (ArgumentNullException). Desktop не сломан
- ⏳ Картинка показывается НЕ на весь экран (масштаб/ориентация) — это 16.3; тач-ввод — тоже 16.3

### 16.3. Ввод и экран ✅ (2026-06-05)

- **Масштаб (letterbox)** ✅: игровая логика осталась в виртуальных 1280×768; на Android back buffer = весь экран (`IsFullScreen=true`, `#if ANDROID`), вся сцена рисуется через матрицу масштаба в единственном `SpriteBatch.Begin(... _renderMatrix)`. `GameManager.UpdateRenderTransform()` (каждый кадр) + `ScreenToVirtual()`. Пропорции сохранены, чёрные поля по бокам. Desktop: scale=1, без изменений
- **Тач** ✅: `TouchPanel` на Android (`#if ANDROID`) вместо мыши и в `InputManager` (игра), и в `Screen` (меню); сырые координаты → виртуальные через `ScreenToVirtual`. Гашение «проклика» первого кадра сохранено
- **Ориентация** landscape (через `MainActivity` ScreenOrientation.SensorLandscape) ✅
- **Автоогонь** ✅: на Android (`#if ANDROID` в `InputManager.HandleScGameInput`) корабль стреляет непрерывно (палец только двигает); на desktop — Space как раньше
- **Кнопка «Назад»** ✅: из игры/экранов → главное меню, из меню/заставки → выход (`GameManager.OnBackRequested` + потокобезопасный `ProcessPendingBack`). На Android 13+ back идёт через predictive back — зарегистрирован `IOnBackInvokedCallback` в `MainActivity` + **`android:enableOnBackInvokedCallback="true"`** в манифесте (без флага система игнорирует колбэк!); fallback `OnBackPressed`/`OnKeyDown` для старых устройств
- **settings.yaml (запись)** ✅: на Android `AppContext.BaseDirectory` = `/data/user/0/<pkg>/files/` (writable внутреннее хранилище), отдельный путь не нужен — снимает 🔶 из 16.1
- **Результат**: проверено на Samsung A56 — меню на весь экран по высоте, «Начать игру» по тапу, корабль следует за пальцем, автоогонь, кнопка «Назад» (игра→меню→выход) ✅

### 16.4. Сборка/запуск и CI 🔶

- Сборка APK в Docker ✅ (`docker/build-apk.sh`, образ `riotgalaxy-android-build`)
- Запуск на устройстве ✅ через `docker/run-on-device.sh` (adb из контейнера, USB-проброс `--privileged -v /dev/bus/usb`, ключ adb в `docker/.home/.android`). Проверено на Samsung A56
- ⬜ Интеграция в CI (этап 6 PRD)
- **Результат**: автоматическая сборка APK + установка/запуск на подключённом устройстве

### 16.5. Отладочный лог (видимый в VSCode и доступный ассистенту) ✅ (desktop-часть)

- Кросс-платформенный `Utils/Log` (Console + файл `riot.log` рядом с приложением; на Android — `Android.Util.Log` → logcat) ✅
- Лог-вызовы: старт уровня (`Level N loaded`), конец игры (`Game over`) — без покадрового спама ✅
- `.vscode/launch.json` + `tasks.json`: F5-отладка DesktopGL (вывод в Debug Console / интегрированный терминал), задача `build-desktopgl` и `run-desktopgl` ✅
- Лог-файл: `MonoGame/RiotGalaxy.DesktopGL/bin/Debug/net6.0/riot.log` (очищается при старте) — ассистент читает его ✅
- Android: `adb logcat -s RiotGalaxy:* > /tmp/riot-android.log` — ассистент читает файл ⬜
- **Результат**: отладочные сообщения видны в VSCode и в `riot.log`, который читает ассистент

## Приоритеты

1. **Критически важные**: Этапы 1-4 (ассеты и основной игровой процесс)
2. **Важные**: Этапы 5-8 (полноценная игровая механика)
3. **Желательные**: Этапы 9-12 (улучшения, полировка, конфигурируемость)
4. **Дополнительные** (доперенос из оригинала): Этапы 13-15 (продвинутые уровни, AI, HUD/эффекты)

> Не переносим (в оригинале мёртвый/неиспользуемый код): `ClusterBomb` (не создаётся),
> `SFX/SfxBlast/SfxGalaxy` (классы есть, но не инстанцируются), платформенные обёртки
> CocosSharp (заменены проектом DesktopGL).

## flexibility

Каждый этап должен завершаться созданием работающей версии игры. Если какой-то этап занимает больше времени, мы можем разбить его на еще более мелкие шаги. Основная цель - сохранение работоспособности проекта на протяжении всей миграции.
